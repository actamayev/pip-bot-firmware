Features:
	- Save multiple wifi networks to preferences. After turning on, the ESP should cycle through the networks until it has established a connection

Optimizations:
	- After successful connection to a WS, the connection fizzles out ()
    - DNS re-direct doesn't work for non-HTTP URLs (only works from HTTP URLs, not something like https://google.com)
    - CA Certificates expire. Upon connecting to wifi, we should immediately check if the certificate is still active.
        If not, it should hit a publicly available S3 bucket to retrieve a overwrite the CA Certifate below.
    - 

Bugs:
	-
