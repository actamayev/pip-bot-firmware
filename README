To run, open in VS Code, and install the `PlatformIO` extension.
Run `Upload and Monitor` under the esp32-s3-devkitc-1/General folders

To add new libraries
	Add them to the `platformio.ini` file or
	Press the home button on the bottom of vsCode, and go to libraries

If getting `Linking .pio/build/local/firmware.elf` errors, run `Full Clean` in platformio

To create a Pip with a new Pip_ID, change the DDEFAULT_PIP_ID in the `platformio.ini` file
